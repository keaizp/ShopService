spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yamaha_shop?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
cos:
  secret-id: ${COS_SECRET_ID}
  secret-key: ${COS_SECRET_KEY}
  bucket: motorcycle-shop-1306776476
  region: ap-shanghai
  base-url: https://motorcycle-shop-1306776476.cos.ap-shanghai.myqcloud.com/
server:
  port: 8080
  servlet:
    context-path: /api